{
    IBClasses = (
        {
            ACTIONS = {
                importTextureImage = id; 
                importVertexData = id; 
                importVertexParameterization = id; 
            }; 
            CLASS = FirstResponder; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}